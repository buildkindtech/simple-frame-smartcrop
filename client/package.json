{
  "name": "smart-crop-standalone",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "setup": "npm --workspace server run setup",
    "dev": "vite",
    "start": "vite",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
